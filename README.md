# GlicoSIM - Sistema de controle glicêmico entre outras ferramentas

https://glicosim.vercel.app

## Rodando localmente

**Prerequisites:**  Node.js


1. Instalando dependências:
   `npm install`
2. Set o `GEMINI_API_KEY` in [.env.local](.env.local) no seu arquivo
3. Rode o comando:
   `npm run dev`
